# Shelf Agent Instructions

- Use Bun and `bunx` for package and script commands.
- Keep the existing Vite web app and shadcn-based UI package structure.
- Use Heroicons for frontend icons.
- Fetch current documentation with `ctx7` before working with external libraries.
- Prefer explicit contracts and hot-cut schema/API changes over compatibility layers unless requested otherwise.
- Do not use emojis or pictographs in code or comments.

